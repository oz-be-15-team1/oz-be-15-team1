name: oz-be-15-team1
# 버전은 생략해도 된다고 합니다
version: '3.9'

services:
  db:
    image: postgres:15-alpine
    restart: always
    environment:
      POSTGRES_DB: budget
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - oz-be-15-team1_db_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    restart: always
    volumes:
      - oz-be-15-team1_redis_data:/data

  web:
    build: .
    command: /scripts/run.sh
    env_file:
      - .env.docker
    ports:
      - "8000:8000"
    depends_on:
      - db
      - redis
    volumes:
      - .:/app
      - oz-be-15-team1_web_venv:/app/.venv

  frontend:
    image: node:20-alpine
    working_dir: /app
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0 --port 5173"
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
    depends_on:
      - web

volumes:
  oz-be-15-team1_db_data:
  oz-be-15-team1_web_venv:
  oz-be-15-team1_redis_data:
